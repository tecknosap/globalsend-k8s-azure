apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: globalsend-site
  namespace: argocd
spec: 
  project: default
  source:
    repoURL: 'https://github.com/tecknosap/globalsend-k8s-azure'
    targetRevision: main
    path: helm/globalsend      # <-- corrected path
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: globalsend-site
  syncPolicy:
    automated:
      prune: true

